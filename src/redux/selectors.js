export const userSelector = (state) => state.auth;

export const noticeAdminSelector = (state) => state.noticeAdmin;

export const playlistSelector = (state) => state.playlist;

export const btnPlaySelector = (state) => state.btnPlay;